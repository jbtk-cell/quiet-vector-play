# Quiet Vector — Free Flight

Play: https://jbtk-cell.github.io/quiet-vector-play/

This repository holds **only the built game**. It is generated — every commit here is
the output of `npm run deploy:free` in the (private) source repo, so nothing should be
edited in place; the change belongs upstream.

Built from source commit `4d7236e`.
